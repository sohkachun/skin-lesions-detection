from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A project to develop an application to classify benign from malignant cases with phone quality skin lesion images and metadata',
    author='sohkachun',
    license='MIT',
)
